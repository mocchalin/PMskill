---
name: prd-reverse-writer
description: >
  开发完成后的 PRD 反写与完工归档技能。从代码仓库自动提取实际实现细节，与原 PRD（如有）做差异比对，
  生成独立的「开发完工报告」，涵盖：实现差异、技术方案沉淀、设计资产归档、变更决策记录。
  支持两种模式：(A) 有原 PRD 的反写模式——自动 diff 计划 vs 实际；
  (B) 无 PRD 直接开发的记录模式——从代码逆向生成完整的需求实现文档。
  当用户提到"反写 PRD"、"开发完工报告"、"开发完成记录"、"PRD 归档"、"实现文档"、
  "代码反推需求"、"记录一下实际做了什么"、"开发完了补文档"、"没有 PRD 帮我补一份"时使用此技能。
  即使用户只是说"项目做完了帮我整理一下文档"，也应使用此技能。
---

# PRD 反写与开发完工报告

开发完成后，从代码仓库逆向提取实际实现，生成结构化的完工归档文档。

这个技能解决的核心问题是：PRD 是"计划做什么"，但开发过程中一定有偏离、取舍、新增——这些变化往往散落在聊天记录、代码注释、和个人记忆里。这个技能的作用是把"实际做了什么"固化下来，成为下一期迭代、交接、复盘的可靠依据。

## 两种工作模式

| 模式 | 适用场景 | 输入 | 核心输出 |
|------|---------|------|---------|
| A — PRD 反写 | 有原 PRD，开发完成后归档差异 | 代码仓库 + 原 PRD | 差异比对 + 完工报告 |
| B — 无 PRD 补录 | 没有 PRD 直接开发，事后补文档 | 仅代码仓库 | 完整的需求实现文档 |

## 工作流程概览

```
路由判断（有/无 PRD） → 代码扫描 → 信息提取 → 差异比对(A) / 需求建模(B) → 生成完工报告 → 用户确认 → 交付
```

### 三层按需加载

| 层级 | 内容 | 何时加载 |
|------|------|---------|
| L0 路由层 | 本文件 | 技能触发时 |
| L1 报告模板 | `references/completion-report-template.md` | 开始生成报告时 |
| L2 设计提取 | `references/design-extraction-guide.md` | 提取 UI/CSS 设计资产时 |

---

## 第一步：路由判断

### 模式 A — PRD 反写

识别信号：
- 用户提供了原 PRD 文件（.md / .docx / 路径）
- 用户提到"和 PRD 比对"、"差异"、"变更"
- 当前 pipeline 中有 final_prd

→ 进入模式 A：差异比对 + 完工报告

### 模式 B — 无 PRD 补录

识别信号：
- 用户没有提供 PRD，只有代码仓库
- 用户提到"没写 PRD"、"先开发后补文档"、"补一份实现文档"
- 用户说"这个项目没有需求文档"

→ 进入模式 B：从代码逆向生成完整需求实现文档

路由不确定时，用一句话确认：

> 这个项目开发前有 PRD 吗？如果有，请提供 PRD 文件，我会做差异比对。如果没有，我会从代码直接生成完整的需求实现文档。

---

## 第二步：收集输入

### 2.1 代码仓库（必选）

使用 AskUserQuestion 收集：

1. **代码仓库路径**：本地路径或当前工作目录
2. **技术栈**（可选，AI 会自动检测）：前端框架、后端语言、数据库等
3. **重点关注模块**（可选）：如果仓库很大，用户可指定只扫描某些目录或模块
4. **Git 历史范围**（可选）：
   - 默认：扫描全部提交
   - 指定时间范围（如"最近 3 个月的提交"）
   - 指定分支（如"feature/smart-recommend 分支"）
   - 指定 tag/commit range（如"v1.0..v1.2"）

### 2.2 原 PRD（模式 A 可选）

- 如果用户在当前 pipeline 中已有 final_prd，自动使用
- 否则询问用户是否提供 PRD 文件

### 2.3 Pipeline 已有产出（可选）

如果当前对话中跑过 prd-pipeline 的其他阶段，以下产出可作为辅助参考：
- Stage 4 的 UI Prompt → 辅助理解设计意图
- Stage 5 的测试用例 → 辅助理解功能边界
- Stage 6 的 UI 还原度报告 → 辅助理解实际 UI 偏差

有就用，没有也不阻塞。

---

## 第三步：代码扫描与信息提取

这是本技能的核心。通过静态分析代码仓库，提取以下六类信息。

### 3.1 项目结构概览

扫描目录结构，识别：
- 项目类型（monorepo / 单体 / 微服务）
- 技术栈（框架、语言、构建工具）
- 模块划分（路由、组件、服务、数据层）
- 配置文件（环境配置、特性开关、第三方服务接入）

**扫描策略**：
1. 先 `ls` 根目录，了解顶层结构
2. 读 `package.json` / `pom.xml` / `go.mod` / `requirements.txt` 等依赖文件，确认技术栈
3. 读 `README.md`（如有），了解项目自描述
4. 递归扫描 2-3 层目录，建立模块树

### 3.2 路由与页面提取

**前端项目**：
- 扫描路由配置（React Router / Vue Router / Next.js pages / Nuxt pages）
- 提取每个路由对应的页面组件
- 记录路由层级关系（父子嵌套、动态参数）
- 识别权限控制（路由守卫、权限 HOC）

**后端项目**：
- 扫描路由/控制器文件（Express / Koa / Spring / Django / Gin）
- 提取 API 端点列表（方法 + 路径 + 中间件）
- 识别鉴权方式（JWT / Session / OAuth）

### 3.3 数据模型提取

- 扫描 ORM 模型定义（Prisma / TypeORM / Sequelize / SQLAlchemy / GORM）
- 提取数据库 schema / migration 文件
- 识别数据关系（一对多、多对多、外键）
- 提取接口 DTO / VO 定义

### 3.4 API 接口提取

- 扫描 API 路由 + 请求/响应类型定义
- 提取 OpenAPI / Swagger 文档（如有）
- 识别第三方 API 调用（axios / fetch / http client 调用）
- 提取错误码定义

### 3.5 设计资产提取（UI/CSS）

扫描前端代码中的设计相关信息：

- **色彩**：从 CSS 变量、Tailwind config、theme 文件中提取色值
- **字体**：从全局样式中提取字体族、字号层级
- **间距**：从 spacing 配置、CSS 中提取间距规范
- **组件**：从组件库导入和使用频率中提取核心 UI 组件清单
- **图标**：从图标库导入中提取使用的图标列表
- **动效**：从 transition / animation 定义中提取动效参数

具体提取指南见 `references/design-extraction-guide.md`（在需要时加载）。

### 3.6 Git 历史分析

从 Git 提交历史中提取：
- **功能提交**：按 feature 分组的提交（conventional commits: feat/fix/refactor）
- **变更热点**：被修改次数最多的文件（暗示需求变更频繁的区域）
- **决策痕迹**：commit message 中的"revert"、"rollback"、"hotfix"、"workaround"
- **TODO/FIXME/HACK**：代码中标注的技术债和待办
- **分支合并记录**：feature 分支的生命周期（从创建到合并的时间跨度）

---

## 第四步 A：差异比对（仅模式 A）

当有原 PRD 时，将第三步提取的实际实现与 PRD 逐项比对。

### 比对维度

| 维度 | 比对方法 | 标记 |
|------|---------|------|
| 功能覆盖 | PRD 中的功能需求编号 vs 代码中的实际实现 | ✅已实现 / ⚠️部分实现 / ❌未实现 / 🆕新增实现 |
| 需求变更 | PRD 中的业务规则 vs 代码中的实际逻辑 | 📝已变更 |
| 数据模型 | PRD 中的字段定义 vs 实际 DB schema | 📝有差异 |
| API 接口 | PRD 中的接口设计 vs 实际实现 | 📝有差异 |
| UI 交互 | PRD 中的交互状态规范 vs 实际组件实现 | 📝有差异 |
| 非功能需求 | PRD 中的性能/安全指标 vs 实际配置 | 📝有差异 |
| 文案 | PRD 中的文案清单 vs 代码中的实际文案 | 📝有差异 |
| 埋点 | PRD 中的埋点设计 vs 代码中的实际埋点 | 📝有差异 |

### 比对策略

1. **逐编号比对**：遍历 PRD 中每个 F-xxx 功能编号，在代码中寻找对应实现
2. **关键词匹配**：用 PRD 中的功能关键词搜索代码注释、变量名、组件名
3. **结构比对**：PRD 中的数据模型表格 vs 实际 model 文件
4. **无法自动确认的**：标记为"❓待确认"，在报告中标注原因，留给用户人工判定

### 差异分类

对每个发现的差异，分类为：

| 差异类型 | 含义 | 示例 |
|---------|------|------|
| 需求砍掉 | PRD 有计划但没做 | PRD 写了"购物车凑单推荐"，代码中没有 |
| 需求降级 | 做了但简化了 | PRD 写"支持 5 种推荐策略"，实际只做了 3 种 |
| 需求新增 | 代码中有但 PRD 没有 | 开发过程中加了一个"推荐屏蔽词过滤" |
| 实现偏离 | 做了但逻辑和 PRD 不同 | PRD 写"超时 500ms 降级"，代码中设的是 800ms |
| 技术调整 | 技术方案变更 | PRD 写用 Redis 缓存，实际用了本地内存缓存 |

---

## 第四步 B：需求建模（仅模式 B）

当没有原 PRD 时，从代码直接构建需求实现文档。

### 建模过程

1. **功能模块划分**：根据路由 + 组件树，划分功能模块
2. **用户故事逆推**：从页面功能和交互逻辑，逆推"作为{角色}，可以{操作}，以便{目的}"
3. **业务规则提取**：从 if/else、验证函数、状态机代码中提取业务规则
4. **边界条件收集**：从输入验证、错误处理代码中提取边界值
5. **数据流梳理**：从 API 调用链 + 状态管理中提取数据流转路径

### 编号规则

没有原 PRD 时，使用以下编号体系：
- 功能模块：M-001, M-002...
- 功能需求：F-001, F-002...
- 子需求：F-001-01, F-001-02...
- API 接口：API-001, API-002...
- 数据模型：DM-001, DM-002...

---

## 第五步：生成完工报告

加载 `references/completion-report-template.md`，按模板生成报告。

### 报告结构

```
{产品名}_开发完工报告.md

文档信息（元数据表）
一、报告概览（文字摘要）
二、功能实现总览（覆盖矩阵）
三、功能模块详述（逐模块）
四、技术方案沉淀
五、设计资产归档
六、变更决策记录
七、技术债与遗留项
八、附件清单
```

### 各章节详细规范

#### 文档信息

表格形式，包含元数据：

| 字段 | 内容 |
|------|------|
| 产品名称 | |
| 报告版本 | v1.0 |
| 开发周期 | {首次提交日期} ~ {最后提交日期} |
| 技术栈 | 前端: / 后端: / 数据库: / 部署: |
| 代码仓库 | {路径或链接} |
| 原 PRD | {文件路径，或"无（模式 B 补录）"} |
| 功能模块数 | {N} 个 |
| 总提交数 | {N} 次 |
| 生成日期 | {当前日期} |

模式 A 额外字段：
| PRD 功能需求数 | {N} 条 |
| 完全实现 | {N} 条 ({%}) |
| 部分实现 | {N} 条 ({%}) |
| 未实现 | {N} 条 ({%}) |
| 新增实现（PRD 外） | {N} 条 |

#### 一、报告概览

1-2 段文字总结：开发了什么、整体完成度如何、最大的变更点是什么。

#### 二、功能实现总览

**模式 A：覆盖矩阵**

| PRD 编号 | 功能名称 | 实现状态 | 代码位置 | 变更说明 |
|---------|---------|---------|---------|---------|
| F-001 | 首页推荐 | ✅已实现 | src/pages/Home/Recommend/ | — |
| F-002 | 详情页推荐 | ⚠️部分实现 | src/pages/Detail/Related/ | 去掉了星级评分 |
| F-003 | 购物车凑单 | ❌未实现 | — | 因排期砍掉 |
| — | 推荐屏蔽词 | 🆕新增 | src/services/FilterService | 开发中新增 |

**模式 B：功能地图**

| 模块编号 | 模块名称 | 页面/接口数 | 核心能力 | 代码位置 |
|---------|---------|-----------|---------|---------|
| M-001 | 用户管理 | 3 页面 + 5 接口 | 注册/登录/权限 | src/modules/user/ |

#### 三、功能模块详述

每个模块包含以下内容：

**模块标题和定位**
- 一句话说明这个模块做什么
- 在系统中的位置和角色

**实现详情**
- 页面/路由列表
- 核心组件清单（组件名 → 文件路径 → 功能描述）
- 业务规则列表（从代码中提取的 if/else 逻辑，用自然语言描述）
- 状态流转（如有状态机，画出实际的状态流转图）

**数据模型**
- 涉及的数据库表/集合
- 字段列表（名称、类型、约束、说明）
- 关联关系

**API 接口**
- 端点列表（方法、路径、功能说明）
- 请求参数（字段、类型、必填、说明）
- 响应格式
- 错误码

**模式 A 额外：与 PRD 的差异说明**
- 逐条列出与 PRD 的差异
- 每条差异说明：原 PRD 怎么写的 → 实际怎么做的 → 变更原因（如能推断）

#### 四、技术方案沉淀

**技术架构**
- 系统架构图描述（文字 + 可选 Mermaid 图）
- 核心技术选型及理由（从代码和注释推断）
- 第三方服务依赖清单

**性能与安全配置**
- 实际的缓存策略（从代码中提取）
- 实际的超时配置
- 实际的安全措施（鉴权方式、加密算法、CORS 配置等）
- 实际的监控/日志配置

**部署与运维**
- 部署方式（从 Dockerfile / CI 配置提取）
- 环境变量清单（脱敏）
- 特性开关清单

#### 五、设计资产归档

从前端代码中提取的实际设计资产：

**色彩体系**

| 角色 | 色值 | 用途 | CSS 变量名 |
|------|------|------|-----------|
| 主色 | #FF6B35 | 品牌色、主要按钮 | --color-primary |
| ... | ... | ... | ... |

**字体层级**

| 层级 | 字体 | 字号 | 字重 | 行高 |
|------|------|------|------|------|
| H1 | Inter | 28px | 700 | 1.3 |
| ... | ... | ... | ... | ... |

**间距规范**

从 Tailwind config 或 CSS 变量中提取的间距体系。

**组件清单**

| 组件 | 来源 | 使用次数 | 文件路径 |
|------|------|---------|---------|
| Button | Ant Design | 47 | — |
| ProductCard | 自定义 | 12 | src/components/ProductCard.tsx |
| ... | ... | ... | ... |

**图标使用清单**

| 图标 | 来源 | 使用场景 |
|------|------|---------|
| ShoppingCart | Lucide | 购物车入口、加购按钮 |
| ... | ... | ... |

**动效参数**

| 场景 | 时长 | 缓动函数 | CSS 属性 |
|------|------|---------|---------|
| 按钮 hover | 200ms | ease-in-out | background-color |
| ... | ... | ... | ... |

#### 六、变更决策记录

从 Git 历史和代码注释中推断的决策记录：

| 编号 | 决策事项 | 决策内容 | 推断来源 | 推断置信度 |
|------|---------|---------|---------|-----------|
| D-001 | 推荐算法 | 从协同过滤改为热门+类目混合 | commit abc123 "switch to hybrid algo" | 高 |
| D-002 | 缓存方案 | Redis → 本地内存 | config 文件 + commit def456 | 中 |
| D-003 | 超时阈值 | 500ms → 800ms | .env 配置值 vs PRD 描述 | 高 |

每条决策的"推断置信度"分三级：
- **高**：有明确的 commit message 或代码注释说明原因
- **中**：可以从代码变更推断，但原因不确定
- **低**：仅从代码差异发现，原因未知

#### 七、技术债与遗留项

**代码中的 TODO/FIXME/HACK**

| 标记 | 内容 | 文件位置 | 严重程度 |
|------|------|---------|---------|
| TODO | 推荐结果缓存策略待优化 | src/services/recommend.ts:142 | 中 |
| HACK | 临时绕过商品中心超时问题 | src/api/product.ts:38 | 高 |
| FIXME | 冷启动比例计算精度问题 | src/utils/coldStart.ts:67 | 低 |

**变更热点**（频繁修改的文件，暗示需求不稳定区域）

| 文件 | 修改次数 | 涉及提交 | 推断原因 |
|------|---------|---------|---------|
| src/services/recommend.ts | 23 | feat/fix/refactor 混合 | 推荐逻辑反复调整 |

**建议下一期处理的事项**
- 从 TODO/HACK 和未实现需求中归纳

#### 八、附件清单

列出所有相关文件路径，方便后续查找：
- 代码仓库路径
- 原 PRD 路径（模式 A）
- Pipeline 其他阶段产出路径（如有）
- 本报告路径

---

## 第六步：用户确认与补充

报告初稿生成后，使用 AskUserQuestion 请用户确认：

> **完工报告初稿已生成**，包含 {N} 个功能模块、{N} 条差异记录、{N} 条决策记录。
>
> 以下内容需要你确认或补充：
>
> 1. 有 {N} 条差异标记为"❓待确认"——是否实现？请逐条确认
> 2. 有 {N} 条决策记录置信度为"低"——你知道具体原因吗？
> 3. 有没有报告中遗漏的重要变更或决策？

选项：
- **我来补充一下** → 用户口头补充，AI 更新报告
- **差不多了，直接生成最终版** → 标记所有"待确认"项为"待后续确认"，生成最终版
- **某些模块需要深挖** → 用户指定模块，AI 做更详细的代码分析

---

## 第七步：交付

### 文件命名

模式 A：`{产品名}_开发完工报告_{YYYYMMDD}.md`
模式 B：`{产品名}_需求实现文档_{YYYYMMDD}.md`

### 交付内容

1. 完工报告主文件（Markdown）
2. 向用户报告摘要：
   - 功能覆盖率（模式 A）或功能模块总数（模式 B）
   - 关键发现（最大的 3 个差异点或技术债）
   - 技术债数量和建议优先级
3. 如果用户在 pipeline 中，更新 pipeline 状态

### 与 pipeline 的集成

如果在 prd-pipeline 中作为 Stage 7 执行：
- 自动使用 pipeline 中的 final_prd 作为比对基准
- 自动使用 pipeline 中的代码仓库路径
- 产出文件加入 pipeline artifacts 列表
- Checkpoint 7 作为新的 pipeline 终点

---

## 代码扫描的注意事项

### 安全边界

- **不执行代码**：只做静态分析，不运行任何脚本或程序
- **不读取敏感文件**：跳过 .env、credentials、private key 等文件
- **环境变量脱敏**：报告中引用环境变量时，只显示 key 名，不显示 value
- **不上传代码**：所有分析在本地完成

### 大型仓库策略

如果代码仓库较大（>1000 个文件），采用以下策略：
1. 先扫描目录结构，建立文件清单
2. 请用户确认重点模块（或自动选择 src/、app/、pages/ 等核心目录）
3. 只深入扫描核心目录，其余目录仅做概要描述
4. 如果用户有 pipeline 的 Stage 4 UI Prompt，用它来确定需要重点分析的页面组件

### 多语言 / Monorepo

- 按 package/module 分别分析，在报告中按模块组织
- 共享库/工具库单独一节描述

---

## 推断置信度说明

报告中很多内容是从代码"推断"出来的，不是用户直接告知的。为了保持诚实和可追溯，遵循以下原则：

1. **标注推断**：所有从代码推断（而非用户明确说明）的内容，在首次出现时标注"（从代码推断）"
2. **给出依据**：每条推断附带依据来源（文件名、commit hash、代码片段引用）
3. **区分事实与推断**：
   - 事实：代码中明确写死的（如配置文件中的值、函数签名）
   - 推断：从代码逻辑推测的（如业务规则、设计意图）
   - 猜测：证据不充分的（标记为"❓待确认"）
4. **不编造原因**：如果无法从代码或 commit message 推断变更原因，就写"原因未知"，不要编

---

## 与其他技能的协作

| 协作技能 | 关系 | 怎么用 |
|---------|------|--------|
| prd-writer | 模式 B 参考 | 模式 B 的需求实现文档可参考 prd-writer 的模板结构 |
| prd-reviewer | 反向对照 | 评审发现的问题是否在代码中解决了？ |
| doc-update-from-feedback | 互补 | 评审反馈的修改是否最终落地？ |
| prd-to-ui-prompt | 设计基准 | Stage 4 的 UI Prompt 作为设计资产的对照基准 |
| prd-test-validator | 测试覆盖 | Stage 5 的测试用例是否覆盖了实际实现的所有功能？ |
| ui-alignment-checker | UI 验证 | Stage 6 的还原度报告作为设计资产准确性的佐证 |
